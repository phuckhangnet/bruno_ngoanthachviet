info:
  name: Lấy chi tiết dịch vụ công thêm theo mã dịch vụ
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/AdditionalService/GetDetail"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
    - name: userIdLogin
      value: "182"
  body:
    type: json
    data: |-
      {
          "ServiceCode": "X000001"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
