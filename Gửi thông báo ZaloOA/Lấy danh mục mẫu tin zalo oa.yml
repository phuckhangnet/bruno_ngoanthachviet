info:
  name: Lấy danh mục mẫu tin zalo oa
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/Common/GetZaloOaTemplate"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
    - name: userIdLogin
      value: "182"
  body:
    type: text
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
