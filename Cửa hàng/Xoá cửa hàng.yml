info:
  name: Xoá cửa hàng
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/Stores/Delete"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
    - name: userIdLogin
      value: "182"
  body:
    type: json
    data: |-
      {
          "StoreId":1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
