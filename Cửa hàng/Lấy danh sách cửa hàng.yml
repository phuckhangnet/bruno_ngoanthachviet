info:
  name: Lấy danh sách cửa hàng
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/Stores/GetAll"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
    - name: userIdLogin
      value: "182"
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
