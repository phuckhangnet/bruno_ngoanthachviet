info:
  name: Cập nhật số lượng
  type: http
  seq: 7

http:
  method: POST
  url: "{{url}}/Products/Quantity"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
    - name: userIdLogin
      value: "2"
  body:
    type: json
    data: |-
      {
          "ProductId":46,
          "Quantity":400
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
