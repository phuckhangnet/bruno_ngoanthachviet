info:
  name: Xoá định danh sản phẩm
  type: http
  seq: 5

http:
  method: POST
  url: "{{url}}/Products/Identity/Delete"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
    - name: userIdLogin
      value: "182"
  body:
    type: json
    data: |-
      {
          "IdentityId":5
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
