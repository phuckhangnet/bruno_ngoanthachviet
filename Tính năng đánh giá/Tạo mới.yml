info:
  name: Tạo mới
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/FeatureFeedback/Create"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
    - name: userIdLogin
      value: "182"
  body:
    type: json
    data: |-
      {
        "Name": "Tính năng chọn ngôn ngữ", 

      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
