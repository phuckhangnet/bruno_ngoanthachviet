info:
  name: Cập nhật
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/FeatureFeedback/Update"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
    - name: userIdLogin
      value: "182"
  body:
    type: json
    data: |-
      {
        "Name": "Tính năng chọn ngôn ngữ cập nhật nè 4", 
        "Id":4,
        "Status":"Active", // Active, Inactive
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
