info:
  name: Lấy danh sách địa chỉ nhận hàng theo khách hàng
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/Order/GetShippingAddresses"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
    - name: userIdLogin
      value: "182"
  body:
    type: json
    data: |-
      {
         "CustomerId":14
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
