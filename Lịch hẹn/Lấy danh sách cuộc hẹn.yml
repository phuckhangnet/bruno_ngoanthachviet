info:
  name: Lấy danh sách cuộc hẹn
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/Appointments/GetAll"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
    - name: userIdLogin
      value: "5"
  body:
    type: json
    data: |-
      {
          "Date": "2025-12-07"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
