info:
  name: Huỷ sản phẩm khỏi phiên đấu giá
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/Auction/Sop/Products/Delete"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
    - name: userIdLogin
      value: "182"
  body:
    type: json
    data: |-
      {
          "AuctionItemId":66,
         "ProductId": 27,
         "Reason":"San pham loi",
         "DamageFee":200000
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
