info:
  name: Quên mât khẩu
  type: http
  seq: 5

http:
  method: POST
  url: "{{url}}/Auth/Customer/ForgotPassword"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
    - name: from
      value: web
      disabled: true
  body:
    type: json
    data: |-
      {
         "Account":"Vu_PKN"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
