info:
  name: Đổi mật khẩu
  type: http
  seq: 6

http:
  method: POST
  url: "{{url}}/Auth/Customer/ChangePassword"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
    - name: from
      value: web
      disabled: true
  body:
    type: json
    data: |-
      {
         "CustomerId":12,
         "OldPassword":"028880",
         "NewPassword":"0901828111"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
