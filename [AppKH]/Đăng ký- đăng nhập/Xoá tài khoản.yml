info:
  name: Xoá tài khoản
  type: http
  seq: 8

http:
  method: POST
  url: "{{url}}/Auth/Customer/DeleteAccount"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
  body:
    type: json
    data: |-
      {
          "CustomerId": 86
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
