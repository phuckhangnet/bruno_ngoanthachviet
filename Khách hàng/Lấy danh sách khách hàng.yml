info:
  name: Lấy danh sách khách hàng
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/Customers/GetAll"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
    - name: userIdLogin
      value: "182"
  body:
    type: json
    data: |-
      {
          "SearchText": "",
          "PageNumber": 1,
          "PageSize": 100,
          "Status": 1, // 1 | 0,
          // "CityAndProvince":"",
          // "District":"",
          // "Dob":"",
          // "IsIdentify": null // true | false | null

      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
