info:
  name: Xoá nhóm
  type: http
  seq: 8

http:
  method: POST
  url: "{{url}}/Employee/Message/Groups/Delete"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
    - name: userIdLogin
      value: "182"
  body:
    type: json
    data: |-
      {
          "GroupId":3

      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
