info:
  name: Lấy danh sách thành viên trong nhóm
  type: http
  seq: 10

http:
  method: POST
  url: "{{url}}/Employee/Message/Groups/Members/GetInfo"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
    - name: userIdLogin
      value: "182"
  body:
    type: json
    data: |-
      {
          "GroupId": 4
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
