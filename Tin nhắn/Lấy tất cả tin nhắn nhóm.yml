info:
  name: Lấy tất cả tin nhắn nhóm
  type: http
  seq: 12

http:
  method: POST
  url: "{{url}}/Employee/Message/Groups/Inboxes"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
    - name: userIdLogin
      value: "182"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
