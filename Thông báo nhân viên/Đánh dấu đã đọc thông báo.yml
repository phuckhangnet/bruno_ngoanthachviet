info:
  name: Đánh dấu đã đọc thông báo
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/NotificationBadge/Staff/MarkAsRead"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
    - name: userIdLogin
      value: "5"
  body:
    type: json
    data: |-
      {
         "Id": 4278
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
