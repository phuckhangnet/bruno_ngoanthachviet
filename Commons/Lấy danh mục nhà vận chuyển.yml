info:
  name: Lấy danh mục nhà vận chuyển
  type: http
  seq: 5

http:
  method: POST
  url: "{{url}}/Common/GetAllActiveShippingCarriers"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
