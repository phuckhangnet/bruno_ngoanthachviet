info:
  name: Generate ThoiGianBaoHanh
  type: http
  seq: 12

http:
  method: POST
  url: "{{url}}/Common/TranslateDonViTinh"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
