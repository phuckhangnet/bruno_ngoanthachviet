info:
  name: Lấy danh mục hình thức thanh toán
  type: http
  seq: 4

http:
  method: POST
  url: "{{url}}/Common/GetAllPaymentMethods"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
