info:
  name: Generate DanhMucLIchHen
  type: http
  seq: 13

http:
  method: POST
  url: "{{url}}/Common/TranslateDanhMucLichHen"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
