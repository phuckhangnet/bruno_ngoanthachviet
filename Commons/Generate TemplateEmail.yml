info:
  name: Generate TemplateEmail
  type: http
  seq: 16

http:
  method: POST
  url: "{{url}}/Common/GenerateTemplateEmail"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
