info:
  name: Lấy danh  mục mã  ngôn ngữ
  type: http
  seq: 9

http:
  method: POST
  url: "{{url}}/Common/GetAudioLanguages"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
