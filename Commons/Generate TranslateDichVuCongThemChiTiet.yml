info:
  name: Generate TranslateDichVuCongThemChiTiet
  type: http
  seq: 25

http:
  method: POST
  url: "{{url}}/Common/TranslateDichVuCongThemChiTiet"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
