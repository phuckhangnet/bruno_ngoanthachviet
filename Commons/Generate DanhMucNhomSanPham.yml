info:
  name: Generate DanhMucNhomSanPham
  type: http
  seq: 14

http:
  method: POST
  url: "{{url}}/Common/TranslateDanhMucNhomSanPham"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
