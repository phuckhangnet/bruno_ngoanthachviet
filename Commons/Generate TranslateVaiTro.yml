info:
  name: Generate TranslateVaiTro
  type: http
  seq: 18

http:
  method: POST
  url: "{{url}}/Common/TranslateVaiTro"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
