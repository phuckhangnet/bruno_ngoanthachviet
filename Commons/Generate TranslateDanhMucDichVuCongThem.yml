info:
  name: Generate TranslateDanhMucDichVuCongThem
  type: http
  seq: 24

http:
  method: POST
  url: "{{url}}/Common/TranslateDanhMucDichVuCongThem"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
