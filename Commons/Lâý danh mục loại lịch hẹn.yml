info:
  name: Lâý danh mục loại lịch hẹn
  type: http
  seq: 3

http:
  method: POST
  url: "{{url}}/Common/GetAppointmentTypes"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
