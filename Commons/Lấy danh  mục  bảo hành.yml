info:
  name: Lấy danh  mục  bảo hành
  type: http
  seq: 10

http:
  method: POST
  url: "{{url}}/Common/GetProductWarranties"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
