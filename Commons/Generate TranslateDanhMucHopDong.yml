info:
  name: Generate TranslateDanhMucHopDong
  type: http
  seq: 21

http:
  method: POST
  url: "{{url}}/Common/TranslateDanhMucHopDong"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
