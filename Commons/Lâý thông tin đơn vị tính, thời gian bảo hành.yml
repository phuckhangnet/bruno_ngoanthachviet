info:
  name: Lâý thông tin đơn vị tính, thời gian bảo hành
  type: http
  seq: 8

http:
  method: POST
  url: "{{url}}/Common/GetCommonInfoForProduct"
  headers:
    - name: x-api-key
      value: pkn
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
