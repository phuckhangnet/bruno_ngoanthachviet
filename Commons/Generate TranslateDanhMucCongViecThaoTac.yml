info:
  name: Generate TranslateDanhMucCongViecThaoTac
  type: http
  seq: 17

http:
  method: POST
  url: "{{url}}/Common/TranslateDanhMucCongViecThaoTac"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
