info:
  name: Generate TranslateDanhMucLoaiMaGiamGia
  type: http
  seq: 22

http:
  method: POST
  url: "{{url}}/Common/TranslateDanhMucLoaiMaGiamGia"
  headers:
    - name: x-api-key
      value: "{{apiKey}}"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
